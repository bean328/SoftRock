# SoftRock default Hyprland settings. Users override these in ~/.config/hypr/hyprland.conf.

monitor = , preferred, auto, 1

env = XCURSOR_SIZE, 24
env = QT_QPA_PLATFORM, wayland;xcb

exec-once = /usr/bin/lxpolkit
exec-once = mako
exec-once = softrock-shell

general {
    layout = dwindle
    resize_on_border = true
}

input {
    kb_layout = us
    follow_mouse = 1
    touchpad {
        natural_scroll = true
    }
}

misc {
    # Render only when something changes: saves power on idle screens.
    vfr = true
    disable_hyprland_logo = true
    disable_splash_rendering = true
    force_default_wallpaper = 0
    background_color = $sr_background
}

# Floating windows by default, so it feels familiar. Super+V toggles tiling per window.
windowrule = float on, match:class .*

$mod = SUPER

bind = $mod, Return, exec, foot
bind = $mod, Space, exec, fuzzel
bind = $mod, E, exec, thunar
bind = $mod, B, exec, firefox-esr
bind = $mod, Q, killactive,
bind = $mod, F, fullscreen,
bind = $mod, V, togglefloating,
bind = $mod SHIFT, E, exit,
bind = , Print, exec, grim -g "$(slurp)" - | wl-copy

bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5

bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

bindel = , XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.5 @DEFAULT_AUDIO_SINK@ 5%+
bindel = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindl = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = , XF86MonBrightnessUp, exec, brightnessctl set 5%+
bindel = , XF86MonBrightnessDown, exec, brightnessctl set 5%-
