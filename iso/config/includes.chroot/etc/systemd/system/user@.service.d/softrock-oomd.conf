# Let systemd-oomd act on user sessions under sustained memory pressure,
# so a runaway app is stopped before the whole desktop freezes.
[Service]
ManagedOOMMemoryPressure=kill
ManagedOOMMemoryPressureLimit=50%
